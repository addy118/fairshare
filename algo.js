const group = {
  subTotal: [250, 50, 290, 60],
  sub1: {
    a: 200,   // 117
    b: 0,     // -83 (A)
    c: 50,    // -33 (A)
  },
  sub2: {
    a: 0,     // -25 (B)
    b: 50,    // 25
  },
  sub3: {
    a: 10,    // -48 (C, D, E)
    b: 40,    // -18 (C)
    c: 80,    // 22
    d: 100,   // 42
    e: 60,    // 2
  },
  sub4: {
    b: 30,    // 10
    c: 20,    // 0
    e: 10,    // -10 (B)
  },
};

// a -> b (25), c (4), d (42), e (2)
// b -> a (83), c (18)
// c -> a (33)
// d -> null
// e -> b (10)

// minimize trans
// a -> d (42), e (2)
// b -> a (58), c (18)
// c -> a (29)
// e -> b (10)